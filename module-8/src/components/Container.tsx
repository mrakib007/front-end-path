const Container = ({children}) => {
    return (
        <div className="w-full px-[20px] max-w-[1220px] mx-auto bg-green-400">
            {children}
        </div>
    );
};

export default Container;